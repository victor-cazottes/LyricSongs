<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/blue</item>       <!-- color when tab -->
        <item name="colorPrimaryDark">@color/dark_blue</item>   <!-- color at the top with phone notifications -->
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppThemeGradientTL_BR" parent="@style/Widget.AppCompat.Toolbar">
        <item name="android:background">@drawable/gradient_one</item>
    </style>

    <style name="AppThemeGradientVerticalTB" parent="@style/Widget.AppCompat.Toolbar">
        <item name="android:background">@drawable/gradient_two</item>
    </style>

    <style name="drawer_order_menu">
        <item name="android:textSize">20sp</item><!-- text size in menu-->
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="AppTheme.NoActionBar." parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
